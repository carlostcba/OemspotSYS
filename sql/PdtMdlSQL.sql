CREATE TABLE PdtMod (
    PdtMdlID INTEGER unsigned NOT NULL DEFAULT 0,
    ModuloID INTEGER unsigned NOT NULL DEFAULT 0,
    Comando VARCHAR(50) NOT NULL DEFAULT '', 
    Inmediato INTEGER unsigned NOT NULL DEFAULT 0,
    PRIMARY KEY (PdtMdlID),   
    INDEX `ModuloID` (`ModuloID`)
    INDEX `Inmediato` (`Inmediato`)
);





















































































































































































































































































































































































































































































































