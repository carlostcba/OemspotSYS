CREATE TABLE Mdl (
    ModuloID INTEGER NOT NULL,
    Nombre VARCHAR(32),
    Descripcion VARCHAR(8192),
    Address INTEGER,
    ModuloEntradaID INTEGER NOT NULL,
    ModuloSalidaID INTEGER NOT NULL,
    GrupoModulos INTEGER,
    OrdenEncuesta INTEGER,
    PRIMARY KEY (ModuloID),
    UNIQUE(Nombre)
);



















































































































































































































































































































































































































































































































