CREATE TABLE RedNod (
    RedID INTEGER NOT NULL,
    NodoID INTEGER NOT NULL,
    Nombre VARCHAR(32),
    SystemParameter INTEGER,
    PRIMARY KEY(RedID, NodoID),
    UNIQUE(RedID, Nombre),
    UNIQUE(Nombre)
);






















































































































































































































































































































































































































































































































