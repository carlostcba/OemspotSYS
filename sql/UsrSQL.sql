CREATE TABLE Usr (
    UsuarioID INTEGER NOT NULL,
    Nombre VARCHAR(32),
    Password VARCHAR(32),
    SystemParameter INTEGER,
    PRIMARY KEY (UsuarioID),
    UNIQUE(Nombre)
);























































































































































































































































































































































































































































































































