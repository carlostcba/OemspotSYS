CREATE TABLE RedNodLnk (
    RedID INTEGER NOT NULL,
    NodoID INTEGER NOT NULL,
    LinkID INTEGER NOT NULL,
    Nombre VARCHAR(32),
    SystemParameter INTEGER,
    PRIMARY KEY(RedID, NodoID, LinkID),
    UNIQUE(RedID, NodoID, Nombre)
);






















































































































































































































































































































































































































































































































