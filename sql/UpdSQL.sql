CREATE TABLE Upd (
    UpdateID INTEGER NOT NULL,
    Nombre VARCHAR(32),
    SystemParameter INTEGER,
    PRIMARY KEY (UpdateID),
    UNIQUE(Nombre)
);
























































































































































































































































































































































































































































































































