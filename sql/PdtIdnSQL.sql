CREATE TABLE PdtIdn (
    PdtIdnID INTEGER unsigned NOT NULL DEFAULT 0,
    ModuloID INTEGER unsigned NOT NULL DEFAULT 0,
    Numero VARCHAR(32) NOT NULL DEFAULT '', 
    Descripcion VARCHAR(32) NOT NULL DEFAULT '', 
    Perfil INTEGER unsigned NOT NULL DEFAULT 0,
    PRIMARY KEY (PdtIdnID),   
    INDEX `ModuloID` (`ModuloID`),
    INDEX `Numero` (`Numero`)
);



















































































































































































































































































































































































































































































































