CREATE TABLE Lic (
    LicenciaID INTEGER NOT NULL,
    Nombre VARCHAR(32),
    SoftwareKey VARCHAR(32),
    Descripcion VARCHAR(8192),
    PRIMARY KEY (LicenciaID),
    UNIQUE(Nombre)
);























































































































































































































































































































































































































































































































