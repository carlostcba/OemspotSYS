CREATE TABLE Dim (
    DimensionID INTEGER NOT NULL,
    Nombre VARCHAR(32),
    Descripcion VARCHAR(8192),
    SystemParameter INTEGER,
    PRIMARY KEY (DimensionID),
    UNIQUE(Nombre)
);























































































































































































































































































































































































































































































































